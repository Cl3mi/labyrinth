package labyrinth.contracts.enums;

public enum ActionType {
    CONNECT,
    START_GAME,
    PUSH_TILE,
    MOVE_PAWN,
    TOGGLE_AI,
    USE_BEAM,
    USE_SWAP,
    USE_PUSH_FIXED,
    USE_PUSH_TWICE
}
